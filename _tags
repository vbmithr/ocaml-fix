true: debug, bin_annot, warn(-40)
<lib>: include
<src>: include

<{lib,src,lib_test}/*.*>: package(ppx_deriving.std), \
                          package(core), \
                          package(conduit.async), \
                          package(nocrypto), \
                          package(bs_devkit), \
                          package(astring), \
                          thread

<top/*.*>: package(compiler-libs.toplevel)