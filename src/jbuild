(library
 ((name fix)
  (public_name fix)
  (modules (fix))
  (libraries (astring))))

(library
 ((name coinbase)
  (public_name fix.coinbase)
  (modules (coinbase))
  (libraries (fix base64 digestif))))

(library
 ((name okcoin)
  (public_name fix.okcoin)
  (modules (okcoin))
  (libraries (fix))))

